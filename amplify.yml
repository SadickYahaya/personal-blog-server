version: 1
applications:
  - backend:
    phases:
      build:
        commands:
          - npm ci
    artifacts:
      baseDirectory: .
      files:
        - '**/*'
    cache:
      paths:
        - node_modules/**/*